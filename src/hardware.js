import * as Capacitor from '@capacitor/core'
import { detect } from 'detect-browser'
import safeArea from 'safe-area-insets'

export default Capacitor

export const Detect = detect

export const SafeArea = safeArea